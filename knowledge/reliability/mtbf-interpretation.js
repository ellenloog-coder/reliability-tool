export const mtbfInterpretationConfig = {
  lowerRandomLimit: 0.9,
  upperRandomLimit: 1.1,
  assumptions: [
    "Exponential / constant failure-rate assumption",
    "MTBF is not the same as product lifetime"
  ]
};
