export const RESULT_CHART_SIZE = Object.freeze({
  full: Object.freeze({ width: 760, height: 250 }),
  split: Object.freeze({ width: 600, height: 250 })
});

export const RESULT_CHART_TYPE = Object.freeze({
  axisFontSize: 9,
  legendFontSize: 8.5
});
